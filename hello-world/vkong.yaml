# App
type: service
app: hello-world
app_port: 8000

# Compute
gpu: Any
num_gpus: 0
cpu_cores: 2
ram_gb: 4
disk_gb: 16
inet_down_mbps: 0

# Budget
max_dph: 0.5
verified_only: true

# Runtime
image: python:3.12-slim
init_cmd: python3 -m pip install -r requirements.txt
start: exec python3 -m uvicorn main:app --host 0.0.0.0 --port "$PORT"
